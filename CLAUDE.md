# ZadGo — تعليمات دائمة

تطبيق توصيل طعام سعودي: Flutter + Firebase، أربع نكهات (عميل/سائق/مطعم/
إدارة) من قاعدة كود واحدة. الواجهة عربية RTL بالكامل.

## اقرأ هذا أولاً في كل جلسة

1. **`dev-docs/binding-rules.md` — الوثيقة المُلزِمة.** بنود اعتمدها
   المالك. راجعها قبل كل دفعة، **ونبّه قبل مخالفة أي بند لا بعدها**،
   بذكر رقم البند ونصّه. إن أصرّ المالك بعد التنبيه فنفّذ ووثّق.
2. **`dev-docs/roadmap-ar.md` — خارطة الطريق.** الأولويات وسجلّ الإنجاز.
   تُحدَّث مع كل دفعة؛ لا يُحذف منها شيء، يُشطب ويُؤرَّخ.

## أهم البنود التي تُخالَف سهواً

- **لا دفع لفرع الإصدار `copilot/split-customer-app` إلا بأمر صريح**
  («ادفع»/«ارفع») في كل مرة. التطوير على
  `claude/tour-marked-branch-1hyzd2`. (أ١، أ٢)
- **✅ رُفع تجميد main بأمر المالك (2026-08-15)** بعد معالجة العمولة —
  يُدمج فرع التطوير في main عند اكتمال الدفعات. **تنبيه: main يحمل موقع
  الويب العام (`docs/`: النطاق، سياسة الخصوصية، حذف الحساب، الأدلة) الذي
  يبنيه مساعد الويب — الدمج ثلاثي الاتجاه يبقيه، فلا force ولا reset على
  main أبداً كي لا يُمحى الموقع.**
- **القواعد: نشرٌ تلقائي عبر CI (`deploy-firestore.yml`) بأمر المالك
  (2026-08-15) — بديلٌ عن أ٥** («القواعد يَنشرها المالك يدوياً»): كل تغيير
  على `firestore.rules`/`firestore.indexes.json` يصل main يُنشر تلقائياً
  عبر سرّ `FIREBASE_SERVICE_ACCOUNT`. لم يعد يلزم إرسال الملف يدوياً ولا
  فتح الكونسول. (يخلف أ٥)
- **راقب كل بناء وأبلغ بنتيجته.** (أ٣)
- **أي حقل مالي جديد يُحمى في القواعد فور إضافته** — لا Cloud Functions،
  فالقواعد خط الدفاع الوحيد. (د١)
- **لا رقم مبرمَج للحوافز** — كل مبلغ يضبطه المدير من لوحته. (ج١)
- **حزمة جديدة = دفعة معزولة** بإصدار مثبَّت. (أ٦)
- **الأدلة الخمسة تُحدَّث مع كل دفعة تمسّ ما تصفه** (`dev-docs/guides/`) —
  أدلةٌ متقادمة أسوأ من غيابها: المستخدم يتّبعها فيصطدم بواقع مختلف.
  حدِّث سطر «آخر تحديث» في رأس الملف مع كل تعديل. (أمر المالك 2026-08-11)

## أدلة الاستخدام — تُحدَّث مع كل دفعة

`dev-docs/guides/` — أربعة أدلة للمستخدمين بلغة عامة، وخامسٌ للمبرمجين:

| الملف | لمن | يُحدَّث حين تمسّ الدفعة |
|---|---|---|
| `دليل-العميل.md` | العميل | شاشات العميل، الأسعار، الإلغاء، المحفظة |
| `دليل-المطعم.md` | مدير المطعم | دورة الطلب، التقارير، المنيو، التعويضات |
| `دليل-الكابتن.md` | الكابتن | العروض، المذكرة، الحوارس، الأرباح، الحوافز |
| `دليل-الإدارة.md` | المدير | المتابعة، السجلّ، المال، المطاعم، الشكاوى |
| `دليل-المبرمج-المبتدئ.md` | مبرمج جديد | البنية، الإسناد، القواعد، الأنماط، الفروع |

## وثائق مرجعية أخرى

- `dev-docs/launch-madinah.md` — خطة الإطلاق الفعلي (الحي الواحد أولاً).
- `dev-docs/store-distribution.md` — استراتيجية النشر على المتاجر.
- `dev-docs/app-signing.md` — بصمة التوقيع وأسرار البناء (سبب رفض
  التحديثات وحلّه).
- `dev-docs/join-page-handoff.md` — مواصفات صفحة تسجيل الكباتن (لمساعد
  الويب على zadgo.co).
- `dev-docs/menu-imports/` — ملفات استيراد المنيوهات الجاهزة.
- `dev-docs/refunds-design.md`، `driver-wallet-design.md`،
  `sync-web-dashboard.md` — تصاميم سابقة.

## أسلوب الكود

- التعليقات بالعربية، وتشرح **لماذا** لا ماذا: القرار الهندسي وبديله
  المرفوض وسببه. طابق كثافة التعليق في الملف الذي تعدّله.
- اختبارات المنطق الخالص في `test/` — يشغّلها CI قبل البناء.
- البناء يدوياً عبر `workflow_dispatch` بمدخل `flavor`، ثم مراقبة النتيجة.
